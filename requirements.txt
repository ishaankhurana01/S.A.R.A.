# S.A.R.A. — full project dependencies, declared once so the environment
# never needs to churn phase-by-phase. See architecture doc §5 for rationale.

# --- Phase 1: core, config, logging, context engine ---
pydantic>=2.6
pyyaml>=6.0
python-dotenv>=1.0
loguru>=0.7
psutil>=5.9
pyperclip>=1.8
pywin32>=306; sys_platform == "win32"

# --- Phase 2: GUI ---
PySide6>=6.6

# --- Phase 3: LLM ---
# OllamaProvider talks to Ollama's REST API directly via `requests` rather
# than the official `ollama` client package — see llm/providers/ollama_provider.py
# docstring for why (smaller surface, easier to mock in tests).
requests>=2.31

# --- Phase 4: Voice ---
faster-whisper>=1.0
openwakeword>=0.6
piper-tts>=1.2

# --- Phase 5: Memory ---
sqlalchemy>=2.0
chromadb>=0.5
sentence-transformers>=3.0
apscheduler>=3.10

# --- Phase 6: Automation ---
pyautogui>=0.9
keyboard>=0.13
mouse>=0.7

# --- Phase 7: Vision ---
opencv-python>=4.9
easyocr>=1.7
mss>=9.0

# --- Testing (all phases) ---
pytest>=8.0
pytest-cov>=5.0

mss>=9.0
